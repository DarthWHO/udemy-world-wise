import styles from "../styles/Map.module.css";

function Map() {
  return (
    <div className={styles.mapContainer}>
      <div className={styles.map}>Test</div>
    </div>
  );
}

export default Map;
